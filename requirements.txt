# Ansible and related tools
# Ansible community package 11+ includes ansible-core 2.18+
# As of Dec 2025: ansible-core 2.18, 2.19, 2.20 are supported (2.16, 2.17 are EOL)
ansible>=11.0.0
ansible-lint>=6.17.0
yamllint>=1.32.0
molecule>=6.0.0
molecule-plugins[docker]>=23.5.0

# VMware dependencies
pyvmomi>=8.0.0
pyvim>=3.0.0
# vSphere Automation SDK (PyPI packages are broken, install from GitHub)
vsphere-automation-sdk @ git+https://github.com/vmware/vsphere-automation-sdk-python.git

# General Python dependencies
requests>=2.31.0
jmespath>=1.0.1
dnspython>=2.4.0

# Testing dependencies
pytest>=7.4.0
pytest-ansible>=4.1.0
